spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/collabflow
    username: zakaria
    password: zakaria
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080
